{
  "dev": {
    "driver": "pg",
    "host": "localhost",
    "user": {"ENV": "PGUSER"},
    "password": {"ENV": "PGPASSWORD"},
    "database": {"ENV": "PGDATABASE"}
  },

  "prod": {"ENV": "DATABASE_URL"}
}
